# Local compose stack configuration. Copy to .env and fill in.
#   cp .env.example .env
# Generate the secret with: openssl rand -base64 36

POSTGRES_USER=realworld
POSTGRES_PASSWORD=
POSTGRES_DB=realworld

JWT_SECRET=
JWT_ISSUER=conduit
JWT_EXP=86400

PUBLIC_URL=http://localhost:8080
